# DECISIONS（ADR 决策记录）

每条决策按 ADR 格式：状态 / 背景 / 决策 / 后果。

---

## ADR-001：以 03 为唯一事实主源

- **状态**：已采纳（2026-04-17）
- **背景**：03 是人工整合多源的成果，01 是官方原始数据，13 是 OCR 产物。若三者并列，冲突时无所适从
- **决策**：03 = SSoT；01 仅补缺 + 校验；13 独立治理后再合入；任何整合不得覆盖 03 既有非空值
- **后果**：
  - ✅ 决策边界清晰，冲突处理有标准
  - ✅ 与历史教训对齐（01 缺提前批、字段翻转，不适合作主源）
  - ⚠️ 若 03 某字段本身有错，依赖交叉校验发现

## ADR-002：不直接入库，先出中间文件

- **状态**：已采纳（2026-04-17）
- **背景**：历史尝试 L2 直接写 DB，scripts/import-data/import-enriched.ts 出 BUG 后难回滚
- **决策**：每阶段产出 xlsx/csv/parquet 中间文件，用户肉眼验收后再考虑入库
- **后果**：
  - ✅ 可回滚、可审计、可并行验收
  - ⚠️ 入库任务推迟到本 spec 之外

## ADR-003：01 独有字段作新增列而非替换

- **状态**：已采纳（2026-04-17）
- **背景**：01 有独家字段（位次、征集标志、压力线），03 部分年份也有位次
- **决策**：01 字段列名加 `_01` 后缀作新增列；若 03 对应字段空则在差异报告中标注"补缺候选"
- **后果**：
  - ✅ 数据血缘清晰
  - ⚠️ 列数增加，需要在数据字典明确区分

## ADR-004：13 OCR 错误率必须先量化再修

- **状态**：已采纳（2026-04-17）
- **背景**：历史尝试直接上修复脚本，未知错误率，修完不知道效果
- **决策**：P3.3 强制先抽样 200 条对原图比对，量化分类错误率，再做修复
- **后果**：
  - ✅ 修复策略有针对性，效果可评估
  - ⚠️ 增加 P3 工时

## ADR-005：本科批模糊 7 条暂标保守值

- **状态**：已采纳（继承 2026-04-17 历史决策）
- **背景**：rename plan 中 7 个条目无法从 xlsx 找到"本科一批/二批控制线"字样
- **决策**：保守标为 `本科批_一二批混合`，P3 阶段由人工复核公告图后定论
- **后果**：
  - ✅ 不误标
  - ⚠️ 需要在 P3 增加人工复核环节

---

## ADR-006：保留 .gitignore 中 data/_pipeline/ 冗余规则

- **状态**：已采纳（2026-04-17）
- **背景**：.gitignore 第 64 行已有 `data/` 宽规则，第 79 行新增 `data/_pipeline/` 技术冗余。code quality reviewer 建议删除
- **决策**：保留新规则 + 显式注释 `# Data integration pipeline intermediate artifacts (not versioned)`
- **理由**：
  - 显式注释让开发者快速理解 `_pipeline/` 是流程中间产物，无需推断 `data/` 宽规则的涵盖
  - 功能无害（实际 ignore 行为由 line 64 生效）
  - 修改 line 64 注释属于 unrelated refactoring
  - spec 明确要求追加此规则
- **后果**：
  - ✅ 配置文件文档价值提升
  - ⚠️ 存在一条技术冗余规则（已备案）

---

## D-P3-001：P3.3 跳过图片比对子 agent，依赖程序化统计

- **状态**：已采纳（2026-04-17）
- **背景**：design.md 原计划用 subagent 对 ~250 样本做图像比对，量化 OCR 字符错误率
- **程序化统计结果**：
  - `number_float_coerced`: 14,463 (52%) — pandas 加载问题，非 OCR 错误，load 层 `dtype=str` 可解
  - `bracket_unbalanced`: 61 (0.22%) — 多为"多行备注跨行"非真正截断
  - `bracket_mixed`: 27 (0.10%) — 中英括号混用
  - `code_malformed`: 25 (0.09%) — 实际多为合法 2 位/字母代码（2K、0T、1A 等），P3.5 主表对齐验证
  - `forward_fill_gap`: 0 — 2023/2024 xlsx 列结构完整，无需 forward-fill
  - `truncated_memo`: 0
- **决策**：不派 subagent 做图像比对
- **理由**：
  - 字符/括号错误率 <0.5%，修复策略已清晰
  - 52% 的"错误"是 loader bug，不是 OCR bug，修 loader 即可
  - 图像比对成本高（~250 图 × 每图 Read token）
  - 若 P3.5 对齐时发现大批不可解释代码，再回补
- **后果**：
  - ✅ P3 工期显著压缩
  - ⚠️ 可能漏识别视觉级错误（如 O→0 字符混淆），P3.5 兜底

## D-P3-002：ADR-004 条款调整 — 错误率量化方式

- **状态**：已采纳（2026-04-17，对 ADR-004 的修订）
- **背景**：ADR-004 要求"抽样 200 条对原图比对"
- **决策**：改为"程序化规则统计 + 如需再图像比对"。保留"先量化再修"的核心原则，放宽实现手段
- **后果**：ADR-004 spirit 保留，letter 更新

## D-P3-003：`页码全为 1` 红旗不成立（TBD-003 关闭）

- **状态**：已采纳（2026-04-17，关闭 TBD-003）
- **背景**：design.md 标注 "页码全为 1" 作为 OCR pipeline bug 嫌疑
- **调查结果**（见 `data/_pipeline/P3/page_one_investigation.md`）：
  - 88 xlsx 中 71 个页码分布正常（multi_page），1 个 all_one，2 个无页码列
  - 页码分布 `5→672, 7→627, 8→616 …` 符合多页 OCR 特征
- **决策**：`all_one` 红旗不成立，不做 "重 OCR / 按序号回填"。仅对唯一的 1 个 all_one 文件在 P3_report 中标注
- **后果**：
  - ✅ 移除无效成本项
  - ⚠️ 那 1 个 all_one 文件需单独观察

## D-P3-004：xlsx 引擎偏好 mimo > claude > 多引擎

- **状态**：已采纳（2026-04-17）
- **背景**：同一页可能有 3 套 OCR 变体：`_mimo-v2-omni.xlsx` / `_claude.xlsx` / `_多引擎.xlsx` (PaddleOCR-VL-1.5)。初版 repair 只跳过 claude，不跳过 多引擎，导致 4414 文件夹 (无 mimo 变体) 输出 1,541 条空码 malformed
- **调查**：`_多引擎.xlsx` 首行为引擎元数据注释 (引擎名/时间/耗时)，第 2 行才是真正表头。pandas 默认将首行当 header，列名解析为 Unnamed:N，`院校代码` 列整列为空
- **决策**：`_select_preferred_engine` 按 `mimo > claude > 多引擎` 优先级去重。同文件夹基名相同时保留最高优先级
- **后果**：
  - ✅ 4414 案例由 1,541 malformed → 正确解析，total malformed 从 1,546 → 5
  - ✅ 命中率 49.1% → 52.4%
  - ⚠️ 若未来需要比对多引擎结果，加 `--include-all-engines` 开关覆盖

## D-P3-005：院校代码多专业行前向填充

- **状态**：已采纳（2026-04-17）
- **背景**：OCR 模式下，同院校首行印 院校代码/名称/地址，后续专业行留空。直接分类会把后续行判为 malformed
- **决策**：`_forward_fill_college` 当且仅当 `专业代码` 非空时下填 `院校代码/名称/地址`。`专业代码` 为空视作分隔行/真空行，不填
- **后果**：
  - ✅ 3,629 条修复记录
  - ✅ 避免了"分隔行误填"反向 bug
  - ⚠️ 已在 `test_repair_df_forward_fill_does_not_fill_real_blank_row` 单测钉死边界

---

_待决策项进入下方，有结论后移到上方_

## D-P4-001：征集志愿作独立关联表 (关闭 TBD-001)

- **状态**：已采纳（2026-04-17）
- **背景**：TBD-001 在 P4.1 决定 13 数据如何接入主表
- **决策**：独立关联表 `征集志愿关联表_2025.xlsx`
- **理由**：
  - 13 有 "轮次" 维度 (第一次/第二次/第三次)，主表主键 (年份, 院校, 专业) 非唯一
  - 13 本质是"事件"(某次征集补录公告)，非"属性"
  - 主表 star schema 保持干净，关联表承载时序事件
- **后果**：
  - ✅ 主表列数受控 (113 列)
  - ✅ 征集多轮次数据无损保留
  - ⚠️ 下游查询需做 JOIN，文档 (data_dictionary.md) 需明确说明

## D-P4-002：2023/2024 holdback 等 Task 7b

- **状态**：已采纳（2026-04-17）
- **背景**：03 主表仅 2025 数据；13 clean 含 2023/2024/2025
- **决策**：13 中 2023/2024 (10,732 行) 产物化为 `征集志愿_历史年份.xlsx`，不纳入 P4 主产物
- **理由**：无 03 主表基础承接历史征集数据；等 Task 7b 完成 01×03 反哺后再重跑 P4 历史切片
- **后果**：
  - ✅ P4 产物自洽
  - ⚠️ Task 7b 完成时需重跑 P4.1-P4.5 包含历史年份

## D-P4-003：血缘独立 JSON，不入主表

- **状态**：已采纳（2026-04-17）
- **背景**：ADR 原则是"每个字段 `_source` 元数据"
- **决策**：行级血缘保留 `_lineage_source` 列 (3 值：03/03+01候选/01)；字段级血缘独立 `血缘.json`
- **理由**：主表已 113 列，若每个字段再加 `_source` 列翻倍到 220+ 列，无法在 xlsx 友好显示
- **后果**：血缘查询需读 JSON；主表保持可用性

---

## 待决策

### TBD-002：最终格式 parquet vs xlsx
- 倾向：双产出（parquet 给程序消费，xlsx 给人工审）
- 推迟到下游消费时决定 (推荐引擎选型期)
