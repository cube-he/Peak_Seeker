# 企业级规划：流程闭环与商用化路线图

> 生成时间：2026-05-18
> 适用范围：VolunteerHelper（四川考生志愿填报系统）
> 基于：对当前代码库（apps/server 27 模块 + 38 张业务表、apps/web 33 路由、services/ocr-service、scripts/data-processing）的完整调研

---

## 0. 现状一句话

后端数据模型与状态机已经覆盖了完整业务流程（38 张业务表 + 27 个模块），**真正缺的是「家长沟通、主管审核 UI、录取归档、征集志愿」四个闭环节点**，以及企业级的横向能力（权限、审计、可观测性）。代码骨架成熟，离「能商用」差临门一脚。

---

## 1. 业务流程完成度矩阵

用户期望流程：报名 → 填资料 → 交老师 → 生成方案 → 家长沟通 → 修改方案（可多次）→ 主管审核 → 定稿 → 填报志愿 → 等待录取通知 → 成功记档案 / 失败征集志愿。

| 阶段 | 后端 | 前端 | 闭环情况 |
|---|---|---|---|
| ① 报名注册 | ✅ `auth` 模块 | ✅ `(auth)/register` | **闭** |
| ② 资料填写 | ✅ `student.service.ts` 692 行 | ✅ `profile/stage/1-3` 1107 行 | **闭** |
| ③ 交老师审核资料 | ✅ `reviewIntake` 状态机 | ✅ 老师端学生详情 | **闭** |
| ④ 老师生成方案 | ✅ `recommend` v4.4 异步引擎 | ✅ `plans/generate` 1730 行 | **闭** |
| ⑤ 家长沟通 | ❌ **无 ParentCommunication 表** | ❌ 无入口 | **断** |
| ⑥ 修改方案（多版本） | ✅ `plan.derive` 派生版本 | ⚠️ UI 可看，改动入口不显眼 | 半 |
| ⑦ 主管审核 | ✅ `startReview / review` 状态机 | ❌ **无主管工作台路由** | **断** |
| ⑧ 定稿 | ✅ `finalize` | ✅ 学生端可见 `FINALIZED` 标 | **闭** |
| ⑨ 实际填报志愿 | ⚠️ 仅有 PDF 导出 | ⚠️ 只能导出 | **半**（合规上无法对接川招考院）|
| ⑩ 等待录取 | ❌ 无进度推送 | ❌ 无页面 | **断** |
| ⑪ 录取归档 | ⚠️ `AdmissionResult` 表已有，**无导入接口** | ❌ 无页面 | **断** |
| ⑫ 征集志愿 | ⚠️ `SupplementaryRecord` 表已有，**无工作流** | ❌ 无入口 | **断** |

**结论**：报名到定稿这条主线 80% 已通；定稿之后整条「实施 + 追踪」链路是空的。

---

## 2. P0 — 流程闭环（建议 3-4 周内完成，不做不算交付）

按 ROI 排序，每一项都是「表已经在，缺接口/UI」。

### 2.1 主管审核工作台（约 1 周，最容易）
- 后端已 ready，前端新增 `apps/web/src/app/(supervisor)/` 路由组
- 包含：待审核列表、批阅页、PlanReview 时间线
- 顺手实现「学生端可见的审核反馈」推送，复用现有 `notification` 模块（SSE）

### 2.2 家长沟通模块（约 1 周，需先对齐边界）
- 新增 `ParentCommunication` 表：`plan_id` / `direction` / `channel`（电话/微信/面谈）/ `summary` / `created_by` / `attachments`
- **决策点**：是做「老师手工录入沟通纪要」还是「接入微信/短信真双向通信」？
  - 前者 3 天，后者 2 周（涉及短信网关 + 微信公众号 + 合规）
  - 建议先做前者
- 关键价值：给主管审核留证据链，给纠纷留底

### 2.3 录取结果归档（约 3-4 天）
- 复用 `data-import` 模块的 xlsx 解析能力，加录取批次结果导入
- 自动匹配 `student_id` + `university_name` + `major_name` → 回填 `AdmissionResult`，状态更新到 `VolunteerPlan`
- 学生端「我的录取」页面 + PDF 归档单

### 2.4 征集志愿工作流（约 1-2 周）
- 表已有（`SupplementaryRecord` / `SupplementarySummary`）
- 缺：未录取学生触发 → 老师生成新方案（复用 recommend 引擎，过滤已被录取专业）→ 主管极简审核 → 学生确认
- **时间敏感**：四川征集志愿窗口通常 24-48 小时，必须有**实时提醒 + 移动端可用**

---

## 3. P1 — 企业级横向能力（1-2 个月）

这些不是"功能"，是"商用化的资格证"。

### 3.1 权限体系完善
- 项目已引入 `casl` 但只 90 行，没真正用起来
- 把"老师只能看自己学生"、"主管能看本机构所有学生"、"管理员跨机构"落到 CASL 策略
- 而不是散落在 service 里的 if-else

### 3.2 审计日志
- 当前关键操作（`plan.finalize` / `studentProfile.update`）只有 `updatedBy` 字段
- 加 `AuditLog` 表 + 拦截器，记录"谁 在 什么时间 对 哪个资源 做了 什么动作 改前改后值"
- **教育行业出事必查这个**

### 3.3 多机构（多租户）支撑
- 现在 `TeacherProfile.isSupervisor` 是单机构假设
- 加 `Organization` 表，所有业务数据带 `org_id`，从一开始就用行级隔离（RLS 或代码中间件）
- **决策点**：你是只服务一家机构，还是要做 SaaS？这影响数据库设计要不要现在改
- **强烈建议现在改**，后期改成本是 10 倍

### 3.4 可观测性
- 日志结构化（pino + JSON）+ 请求 trace id 贯穿全链路
- Prometheus + Grafana：QPS / 错误率 / 推荐引擎耗时 / 数据库慢查
- Sentry 接前端错误（05-12 那次部署事故肯定能更早发现）

### 3.5 数据安全与合规
- 学生身份证、电话号码加密存储（字段级 AES）
- PII 字段访问加审计
- 备份策略：MySQL 每日全量 + binlog 增量，异地存放
- 这些是教育行业卖给学校的硬要求

### 3.6 测试补齐
- 当前 85 个测试文件，重点在查询路径
- 补全 plan 工作流的状态机测试（每个状态转换 + 非法转换都要测）
- 补全权限测试（老师 A 不能看老师 B 的学生）

---

## 4. P2 — 平台化与商业化（3-6 月）

只在 P0/P1 完成后才考虑。

- **微信小程序 / 移动 H5**：家长不会用 PC，征集志愿窗口期更是手机为主
- **AI 客服 / 智能问答**：把现有 `ai` 模块升级为面向学生家长的 RAG 助手
- **报表与 BI**：机构主管要看「本届学生录取率分布、本机构定稿率、老师工作量」
- **付费/订阅体系**：项目已有 `user.vip` 字段但无对应工作流
- **跨届归档**：现在批次配置以 2026 为锚点，下一届怎么平滑过渡需要从 schema 上设计

---

## 5. 不建议做（避免误投入）

- ❌ **不要新引入微服务**：当前单体 NestJS 完全够，过早拆分是债不是资产
- ❌ **不要重写 OCR 服务**：`OCR.md` 已经识别清楚五大错误根源，迭代规则即可
- ❌ **不要追求"对接四川招考院 API"**：教育考试院没有公开开放给第三方的填报接口，做了也用不了，**只能停留在"打印填报清单给学生抄到官网"**
- ❌ **不要做 `plan-candidate` 那种 1511 行的 service**：业务边界模糊，下一次迭代时建议拆解

---

## 6. 需要拍板的决策点

| # | 决策 | 影响 |
|---|---|---|
| A | 家长沟通：先做「手工纪要」还是「真双向通信」？ | P0 工期 3 天 vs 2 周 |
| B | 给一家机构做，还是做 SaaS 卖给多个机构？ | 决定要不要现在就上 `org_id` 多租户 |
| C | 是否需要移动端（小程序/H5）？ | 决定 P0 后第一站是补移动还是补 BI |
| D | 商业模式是机构按席位付费，还是学生 C 端付费？ | 影响付费/权限/合规设计 |

---

## 7. 推荐启动顺序

| 序号 | 模块 | 工期 | 前置 |
|---|---|---|---|
| 1 | 主管审核工作台 | 1 周 | 无 |
| 2 | 家长沟通（手工纪要版） | 3-5 天 | 决策点 A |
| 3 | 录取结果归档 | 3-4 天 | 无 |
| 4 | 征集志愿工作流 | 1-2 周 | #3 |
| 5 | 审计日志 + 权限收口 | 1 周 | 无 |
| 6 | 多租户改造（如做 SaaS） | 2-3 周 | 决策点 B |
| 7 | 可观测性三件套 | 1 周 | 无 |
| 8 | 移动端 H5 / 小程序 | 4-6 周 | 决策点 C |

---

## 8. 调研依据（可追溯）

- **后端模块**：`apps/server/src/modules/`（plan 626 行 / student 692 行 / recommend 604 行 / admission 529 行）
- **数据模型**：`apps/server/prisma/schema.prisma`（13 次迁移、38 张业务表）
- **前端路由**：`apps/web/src/app/`（33 个页面，按 `(auth)/(student)/(teacher)/(admin)/(main)/(user)` 路由组隔离）
- **数据导入**：`scripts/data-processing/`（10 步全流程，源文件 `2026四川高考志愿--专家版合并整理v2.xlsx`，48132 条记录）
- **部署**：`docker-compose.yml`（MySQL 8 + Redis 7 + Nest + Next + Nginx）+ `ecosystem.config.js`（PM2，端口 3003/3004/8100）
- **OCR 服务**：`services/ocr-service/main.py`（Baidu/PaddleOCR-VL/Docker PaddleOCR/RapidOCR/AIStudio 五引擎）
- **历史规划**：`项目规划文档.md` / `项目实施方案.md` / `数据库设计详细文档.md` / `【系统方案】2026二诊模拟志愿预案-工作流程与架构.md` / `docs/superpowers/plans/`（40+ 阶段性规划）
- **已知事故**：`AGENTS.md` 2026-05-12 部署事故（前端构建期 `NEXT_PUBLIC_*` 变量缺失）

---

## 9. 下一步

任选一项启动，对应将进入 `superpowers:brainstorming` 流程产出可执行设计方案：

1. 主管审核工作台（最快见效）
2. 家长沟通 + 录取归档（闭环主线）
3. 多租户重构（如果要做 SaaS）
4. 权限 + 审计 + 可观测性三件套（企业资质）
